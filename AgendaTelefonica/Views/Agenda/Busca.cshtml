@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}
<div class="row">
    <div class="col-md-4">
        <form asp-action="ShowSearchResults">
            <div class="from-group">
                <label for="TermoDeBusca" class="control-label"></label>
                <input name="TermoDeBusca" class="form-control" />
            </div>
            <div class="form-group">
                <input type="submit" value="Busca" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
